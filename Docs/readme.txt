To get started
1. clone the repo
2. npm install server and client apps
3. set jwt key in the command prompt[make sure to use setx jwtkey <value> e.g setx jwtkey 12345 and restart all command prompts.
4. keep the mongodb server running
